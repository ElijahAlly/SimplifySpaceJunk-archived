/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Use the prefix for GitHub Pages
  basePath: '/SimplifySpaceJunk',
  assetPrefix: '/SimplifySpaceJunk/',
  output: 'standalone',
}

module.exports = nextConfig
